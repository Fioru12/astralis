# Contributing

Thanks for your interest! To contribute:

1. Fork the repo and create a branch for your feature/fix.
2. Run `npm install` and `npm run dev` to test locally.
3. Follow the code style (run `npm run lint` and `npm run format`).
4. Open a PR with a clear description.

Be respectful and include tests for bug fixes or new features.
